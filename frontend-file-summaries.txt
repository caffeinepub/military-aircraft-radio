{
  "frontend/src/index.css": "Global styles with OKLCH monochrome dark/light palette, Space Grotesk body font, Space Mono heading font applied to h1/h2/h3 and `.font-heading` class, and scrollbar overrides.",
  "frontend/tailwind.config.js": "Tailwind config with Space Grotesk as the sans font and Space Mono as the heading/mono font, replacing Inter and Fredoka One, with all existing color tokens and animation keyframes preserved.",
  "frontend/src/services/radioBrowserApi.ts": "Radio Browser API service with multi-host fallback, localStorage caching (key `antenna_stations_v1` with timestamp), a hardcoded fallback list of 17 popular global stations, and a `StationsResult` type (`{ stations, source: 'live'|'cache'|'hardcoded' }`). `fetchTopStations` and `searchStations` now return `StationsResult` and fall back through cache → hardcoded when all API hosts fail; `fetchGlobeStations` similarly falls back without throwing.",
  "frontend/src/hooks/useRadioPlayer.ts": "Enhanced radio player hook with Web Audio API AnalyserNode for visualizer data, stream health state (healthy/stalled/reconnecting/failed), auto-retry with exponential backoff (up to 3 attempts), stall detection, and localStorage persistence of the last-played station.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for backend favorites CRUD operations (getFavorites, addFavorite, removeFavorite) with cache invalidation, plus a utility to convert RadioStation API objects to backend Station types.",
  "frontend/src/components/RadarWidget.tsx": "Canvas radar widget now accepting an `amplitude` prop (0–1) that modulates sweep speed, trail brightness, sweep line glow, and blip intensity in real-time alongside the existing playback state logic.",
  "frontend/src/components/GaugeWidget.tsx": "Canvas-based circular gauge/dial widget displaying a value arc, tick marks, and needle in phosphor green HUD style with configurable min/max/label/unit props.",
  "frontend/src/components/AppHeader.tsx": "Minimal app header with Fredoka One heading font for the \"Antenna\" title, search toggle, and three named-export view tabs (Stations, Saved, Globe with icon) using the updated `ActiveView` type.",
  "frontend/src/components/NowPlayingPanel.tsx": "Now-playing panel showing station name, status, and audio visualizer; includes all 33 cycling slogans that fade in/out every 5 seconds — shown as the idle message when no station is selected, and as a small italic label on wider screens when a station is active.",
  "frontend/src/components/SearchPanel.tsx": "Minimal search panel with clean rounded inputs, pill-style genre tags, country autocomplete, and monochrome styling — no HUD borders or decorative elements.",
  "frontend/src/components/StationList.tsx": "Station list component with an added `stationSource` prop and a `SourceBanner` sub-component that renders a subtle, non-intrusive banner (\"Showing cached stations\" or \"Using fallback stations — API unavailable\") above the list when the source is `cache` or `hardcoded`; no banner is shown for live data.",
  "frontend/src/components/HUDLayout.tsx": "Thin pass-through wrapper using the standard bg-background token with no decorative effects.",
  "frontend/src/App.tsx": "Main app component with two distinct layout modes — globe view uses a full `100dvh` container with the header and toolbar absolutely overlaid on top of the globe, while stations/favorites views use the standard stacked flex layout with bottom padding for the fixed toolbar.",
  "frontend/index.html": "HTML entry point with manifest link, theme-color meta, Apple PWA meta tags, favicon, and apple-touch-icon all referencing the user-provided antenna image from ICP storage, alongside the existing font imports.",
  "frontend/src/hooks/useTheme.ts": "Custom hook managing dark/light theme state persisted to localStorage, toggling the `.light` CSS class on the document root.",
  "frontend/src/components/AudioVisualizer.tsx": "Canvas-based frequency bar visualizer using monochrome greyscale colors (light grey to white) instead of green/amber HUD colors, with ResizeObserver for flexible sizing.",
  "frontend/src/components/BottomToolbar.tsx": "Minimal bottom toolbar now using `fixed bottom-0 left-0 right-0 z-40` positioning so it stays pinned to the viewport bottom across all views without scrolling; all playback, volume, fullscreen, theme, and menu controls are unchanged.",
  "frontend/src/hooks/useWakeLock.ts": "Custom React hook that manages the Screen Wake Lock API, requesting a screen lock when `enabled` is true and releasing it on cleanup or when disabled, with full error handling and visibility-change re-acquisition.",
  "frontend/src/components/FullscreenOverlay.tsx": "Fullscreen player overlay with globe background, audio visualizer, station info, cycling slogans, and status indicators — the unintended mascot image has been removed, leaving only intentional UI elements.",
  "frontend/src/utils/geoMapping.ts": "Utility module mapping country names and ISO 2-letter codes to lat/lng coordinates, with a `getCountryCoordinates` function for globe positioning.",
  "frontend/src/components/GlobeView.tsx": "Full-viewport 3D interactive globe using `absolute inset-0` positioning; the top label text overlay has been removed so only the hover tooltip (shown only on cursor hover) and the 3D globe with station markers remain, eliminating the conflict with the AppHeader in global mode.",
  "frontend/src/components/FullscreenGlobe.tsx": "Fullscreen 3D globe background component that is fully light/dark reactive; the yellow pulsing dot and expanding ring animations have been completely removed — the station marker is now a static monochrome sphere (white in dark mode, dark blue in light mode) with no animation.",
  "frontend/public/manifest.json": "PWA web app manifest for Antenna with standalone display mode, dark theme color, and two icons both referencing the user-provided antenna image from ICP storage."
}