# Specification

## Summary
**Goal:** Update the app title to "SITUATION MONITOR RADIO" everywhere it appears in the UI.

**Planned changes:**
- Update the AppHeader to display "SITUATION MONITOR RADIO" as the app title
- Update the browser tab/document title (index.html) to "SITUATION MONITOR RADIO"
- Remove any remaining references to the old app name in the UI

**User-visible outcome:** The app header and browser tab both read "SITUATION MONITOR RADIO" with no traces of the previous title.
